# critic:provisioning

Ordered from empty /Users/sonnysangha/Documents/Builds/n8n-clone-demo (verified empty; clerk whoami = sonny.sangha@gmail.com with linked:null; vercel whoami = sonnysangha on CLI 54.18.6; ~/.convex/config.json has accessToken) to a deployed app across the 12 phases. Chosen provisioning route (deviates from the Vercel-Marketplace route the findings also describe): CLI-direct for Clerk (`clerk apps create` + `clerk enable orgs`; the Marketplace Clerk product cannot enable Organizations and cannot adopt an existing app) and CLI-direct for Convex (`npx convex dev --once --configure new --team <slug> --project papaflow --dev-deployment cloud`; the Marketplace route forces a separate Convex team plus a browser `npx convex login --vercel`). Marketplace is kept only as an optional path for Resend and requires a TTY for accept-terms. Blockers before Phase 1: scaffold (create-next-app aborts if CLAUDE.md is present), Clerk app + orgs + env pull, Clerk Convex-integration activation (dashboard-only), Convex project + `CLERK_FRONTEND_API_URL`, Clerk webhook endpoint (dashboard-only on CLI 3.2.0) + signing secret, git/gh repo. Vercel project creation is a Phase-1 blocker only because later phases need the stable HTTPS origin; the first `vercel deploy` is always production. Every dashboard-only step, every user-supplied secret, the full env matrix (where each var lives and first phase), the research gaps, and the ordering constraints are itemised below.

- [blocker] P0.1 CLI: pre-flight checks (repo dir, toolchain, logins)
  Run in /Users/sonnysangha/Documents/Builds/n8n-clone-demo: `ls -la` (must contain only allowlisted entries: .git .gitignore .claude .vscode docs LICENSE; CLAUDE.md/AGENTS.md abort create-next-app) ; `node --version` (24.14.1; eve needs >=24) ; `pnpm --version` (11.24.0) ; `clerk --version` (3.2.0; `clerk update` to 3.3.0 is optional but re-run `clerk api ls webhook` afterwards to see whether an endpoints route appeared) ; `clerk whoami` (expects email sonny.sangha@gmail.com, linked:null) ; `vercel whoami && vercel teams ls` (sonnysangha; team slug sonnysanghas-projects) ; `gh auth status` ; `node -e 'console.log(Object.keys(JSON.parse(require("fs").readFileSync(process.env.HOME+"/.convex/config.json"))))'` (expects [accessToken]) ; `npx convex login status` (allowed at execution time; prints 'Teams: N teams accessible' then '  - <name> (<slug>)' -> record <convex-team-slug>).
  RESOLUTION: All CLI. Abort if any login is missing; `clerk login`, `vercel login`, `gh auth login`, `npx convex login` are interactive browser flows the user must run themselves.
- [blocker] P0.2 CLI: scaffold Next.js 16.3.4 into the current directory
  cd /Users/sonnysangha/Documents/Builds/n8n-clone-demo && `pnpm create next-app@16.3.4 . --yes --ts --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-pnpm --disable-git --no-agents-md` (appName = n8n-clone-demo, valid; do NOT pass --turbopack, it is not an option; Turbopack is default). Then `cp /Users/sonnysangha/Downloads/papaflow/CLAUDE.md . && mkdir -p docs && cp /Users/sonnysangha/Downloads/papaflow/docs/*.md docs/`. Edit package.json: add `"engines": {"node": "24.x"}`, scripts typecheck `tsc --noEmit`, test `vitest run`, convex:dev `CONVEX_ALLOW_ANONYMOUS=false npx convex dev`, workflow:web `npx workflow web`. Pin: `pnpm add next@16.3.4 react@19.2.8 react-dom@19.2.8 convex@1.45.0 @clerk/nextjs@7.8.4 @clerk/backend@3.17.0 @clerk/ui@1.31.0 @xyflow/react@12.11.6 zod@4.5.4 ai@7.0.90 @ai-sdk/openai@4.0.56 @ai-sdk/anthropic@4.0.48 @ai-sdk/google@4.0.62 @ai-sdk/groq@4.0.37 resend@6.25.0 next-themes@0.4.6 lucide-react@1.39.0 sonner@2.0.8 class-variance-authority@0.7.1 tailwind-merge@3.6.0 clsx@2.1.1` and `pnpm add -D typescript@6.0.3 @types/node@24.13.3 @types/react@19.2.18 @types/react-dom@19.2.5 tailwindcss@4.3.3 @tailwindcss/postcss@4.3.3 eslint@9.39.5 eslint-config-next@16.3.4 vitest@4.1.11 convex-test@0.0.56 @edge-runtime/vm@5.0.0 vite-tsconfig-paths@6.1.1`. Do NOT install @clerk/themes (Core 2 line), eslint 10, typescript 7, workflow or eve yet. Then `pnpm dlx shadcn@4.20.0 init -d` (Base UI default, style base-nova; sonner exists there) and `pnpm dlx shadcn@4.20.0 add -y button input dialog sheet dropdown-menu badge tabs tooltip sonner card select textarea label separator scroll-area command popover switch table skeleton`.
  RESOLUTION: All CLI, in the project directory. Note Next 16 uses proxy.ts (export `config` matcher) not middleware.ts; use next.config.mts once eve is added (Phase 10) because eve/next is ESM-only.
- [blocker] P0.3 CLI: git repo + GitHub remote
  `git init -b main && git add -A && git commit -m "chore: scaffold Next.js 16 + shadcn + React Flow"` then `gh repo create n8n-clone-demo --private --source=. --push --remote=origin` (gh 2.91.0 supports these flags). Ensure .env.local and .workflow-data/ and .eve/ are gitignored (create-next-app gitignore covers .env*; add `.workflow-data/` and `.eve/` now).
  RESOLUTION: CLI. Needed before `vercel git connect` (P0.9).
- [blocker] P0.4 CLI: create Clerk application, enable Organizations, pull dev keys
  `clerk apps create "PapaFlow" --json` (read application_id from JSON; secret keys are stripped) ; `clerk link --app <application_id>` (run inside the project dir) ; `clerk enable orgs --max-members 5 --yes` (add `--force-selection` if every user must have an active org; PLAN treats solo users as an org of one) ; `clerk config pull --keys organization_settings` to confirm ; `clerk env pull --file .env.local` -> writes NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (pk_test_) and CLERK_SECRET_KEY (sk_test_). Also write into .env.local: NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in, NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up (optional).
  RESOLUTION: CLI. Must precede P0.6 (Convex integration is per Clerk app) and P0.8 (webhook). Note `clerk enable billing` is deliberately deferred to Phase 11 because it auto-creates the free_org plan and needs a claimed app.
- [blocker] P0.5 CLI: create Convex project + cloud dev deployment
  First write a minimal `convex/schema.ts` (defineSchema with the CLAUDE.md tables, each `.index("by_org", ["orgId"])`) so the push has modules. Then `npx convex dev --once --configure new --team <convex-team-slug> --project papaflow --dev-deployment cloud` (uses ~/.convex accessToken; `--configure new` bypasses the non-TTY anonymous-local branch; `dev` has no --yes). It writes .env.local: `CONVEX_DEPLOYMENT=dev:<adjective-animal-123>` and `NEXT_PUBLIC_CONVEX_URL=https://<adjective-animal-123>.convex.cloud`, plus convex/README.md, convex/tsconfig.json, convex/_generated/. Record `<deployment>` -> the HTTP actions origin is `https://<deployment>.convex.site` (needed for the Clerk webhook URL). All later pushes: `CONVEX_ALLOW_ANONYMOUS=false npx convex dev --once`.
  RESOLUTION: CLI. Alternative if the flag combo misbehaves: `npx convex project create papaflow --team <slug>` then `npx convex deployment create --type dev --select` (confirm flags with `--help` first).
- [blocker] P0.6 MANUAL: activate the Clerk native Convex integration (dashboard-only) and set the issuer on Convex
  Dashboard: https://dashboard.clerk.com/apps/setup/convex (select the PapaFlow app, development instance) -> click 'Activate Convex integration' -> copy the revealed Frontend API URL (format https://<verb-noun-00>.clerk.accounts.dev). The aud=convex claim is pre-mapped; no JWT template. No CLI/Platform API route exists for this (config schema has no integrations key). Then CLI: `npx convex env set CLERK_FRONTEND_API_URL https://<verb-noun-00>.clerk.accounts.dev` ; write `convex/auth.config.ts` = `{ providers: [{ domain: process.env.CLERK_FRONTEND_API_URL!, applicationID: "convex" }] } satisfies AuthConfig` ; `CONVEX_ALLOW_ANONYMOUS=false npx convex dev --once`. Afterwards run `clerk config pull --keys session` and record how the aud claim is stored (if it lands in session.claims, future setups can do `clerk config patch --json '{"session":{"claims":{"aud":"convex"}}}' --yes`). Fallback if the dashboard step is skipped: `clerk api /jwt_templates -X POST -d '{"name":"convex","claims":{"aud":"convex"}}' --yes` (ConvexProviderWithClerk falls back to template 'convex' automatically) but template tokens lack pla/fea so Phase 11 plan claims would not reach Convex.
  RESOLUTION: MANUAL dashboard step by the user, then CLI. Requires P0.4 (Clerk app) and P0.5 (Convex deployment) to exist.
- [blocker] P0.7 CLI: generate app-owned secrets (ENGINE_SECRET, CREDENTIALS_KEK)
  `openssl rand -base64 32` twice. ENGINE_SECRET -> .env.local AND `npx convex env set ENGINE_SECRET <value>` (needed from Phase 2 by the shared-secret public mutation; setting it now avoids a mid-phase env change). CREDENTIALS_KEK (32 bytes base64, Next-side only, AAD `${orgId}:${connectionId}`) -> .env.local; first used Phase 4. Pipe values via stdin/`--value` to keep them out of shell history; the same values go to Vercel in P0.9.
  RESOLUTION: CLI. Generate once; the KEK must never rotate without a `keyId` migration.
- [blocker] P0.8 MANUAL: Clerk webhook endpoint to the Convex httpAction
  Write `convex/http.ts` with route POST `/clerk-webhook` (verify with `verifyWebhook` from `@clerk/backend/webhooks` reading CLERK_WEBHOOK_SIGNING_SECRET, or svix 2.2.0 + JSON.parse the raw body) and push it. Then create the endpoint. CLI 3.2.0 only exposes /webhooks/svix routes, so: first try `clerk api ls webhook`; if an endpoints route exists (docs claim it on newer CLIs) use `clerk api <path> -X POST -d '{"url":"https://<deployment>.convex.site/clerk-webhook","subscribed_events":[...]}' --yes` and read the secret from the response. Otherwise MANUAL: https://dashboard.clerk.com -> PapaFlow -> Configure -> Webhooks -> Add Endpoint -> URL `https://<deployment>.convex.site/clerk-webhook` -> subscribe organization.created/updated/deleted, organizationMembership.created/updated/deleted, subscription.*, subscriptionItem.* -> Create -> copy Signing Secret (whsec_...). Then `npx convex env set CLERK_WEBHOOK_SIGNING_SECRET whsec_...`. Local dev relay (optional): `clerk webhooks listen --token "$(clerk webhooks token)" --forward-to http://localhost:3000/api/webhooks` registers a Relay URL as a second endpoint.
  RESOLUTION: MANUAL (dashboard) unless `clerk api ls webhook` on CLI >=3.3.0 shows an endpoints route. Requires P0.5 for the .convex.site host. Convex dev deployment URLs are public, so no tunnel is needed.
- [blocker] P0.9 CLI+MANUAL: Vercel project, link, env vars, git connect, first deploy
  CLI (54.18.6): `vercel project add papaflow --scope sonnysanghas-projects` ; `vercel link --yes --scope sonnysanghas-projects --project papaflow`. Build command: no `vercel project update` on 54.18.6, so either MANUAL Dashboard > papaflow > Settings > Build and Deployment > Framework Next.js, Build Command `npx convex deploy --cmd 'pnpm build' --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL`, or commit `vercel.ts` (`import type { VercelConfig } from '@vercel/config/v1'`; `pnpm add -D @vercel/config@0.7.0`) with framework 'nextjs' and that buildCommand. Node 24.x: already default; package.json engines pins it. Fluid: default-on for new projects (verify Settings > Functions). Env vars (one command per environment; never combine development with production/preview): `vercel env add NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY production --value pk_test_... --yes` and preview (dev-instance keys serve production until the Clerk prod instance exists, see P11.4); same for CLERK_SECRET_KEY, ENGINE_SECRET, CREDENTIALS_KEK. Convex deploy keys are MANUAL: Convex Dashboard > papaflow > production deployment > Deployment Settings > General > Generate Production Deploy Key -> `vercel env add CONVEX_DEPLOY_KEY production --value 'prod:...' --yes` ; Convex Dashboard > Project Settings > Generate Preview Deploy Key -> `vercel env add CONVEX_DEPLOY_KEY preview --value 'preview:...' --yes`. Do NOT set NEXT_PUBLIC_CONVEX_URL or CONVEX_DEPLOYMENT on Vercel (convex deploy injects the URL at build). Convex production env: `npx convex env set --prod CLERK_FRONTEND_API_URL ...`, `--prod ENGINE_SECRET`, `--prod CLERK_WEBHOOK_SIGNING_SECRET` (a second Clerk webhook endpoint pointing at the prod .convex.site host, created as in P0.8, is needed once prod is used). Git: MANUAL once in browser: install the 'Vercel for GitHub' App with access to sonnysangha/n8n-clone-demo (personal repo requires Owner), then `vercel git connect --yes`. Deploy: `vercel deploy --dry --format=json` then `vercel deploy --yes` (FIRST deployment of a new project is ALWAYS production) -> record `https://papaflow-<hash>.vercel.app` / `https://papaflow.vercel.app` as APP_ORIGIN; this HTTPS origin is a hard input for Phases 5-7, 10, 12.
  RESOLUTION: CLI plus three MANUAL steps (build command if no vercel.ts, two Convex deploy keys, GitHub App install). Creating the project in Phase 1 is deliberate: OAuth redirect URIs, Slack/Discord endpoint URLs and eve's Client host all need the stable origin.
- [major] P2.1 CLI: Workflow SDK install + Next/Clerk wiring (Phase 2)
  `pnpm add workflow@5.0.0-beta.47` (NOT `npm i workflow`, which installs 4.8.5; re-check `npm view workflow dist-tags --json`). next.config: `export default withWorkflow(nextConfig)` from 'workflow/next'. proxy.ts matcher must exclude `.well-known/workflow/` (Clerk's default matcher intercepts POST /.well-known/workflow/v1/flow and the Local World fails with 'detached ArrayBuffer'). Local dev: `pnpm dev` alone starts the Local World (data in ./.workflow-data); inspector `npx workflow web`; `npx workflow health`. On Vercel no WORKFLOW_* env vars; Vercel World is auto-selected. Read v5 docs at workflow-sdk.dev/v5/docs/* (unversioned pages are v4). Optional `pnpm add -D @workflow/vitest@5.0.0-beta.47`.
  RESOLUTION: CLI. No provisioning on Vercel; Hobby includes 50k events/month.
- [major] P2.2 SECRET-FROM-USER + MANUAL: Resend API key and sending domain (Phase 2, Send email node)
  No CLI can mint the first key: user creates it at https://resend.com/api-keys (Full access, bootstrap; shown once) -> RESEND_API_KEY into .env.local and `vercel env add RESEND_API_KEY production|preview|development --value ... --yes`. Before any domain is verified, Resend only delivers from onboarding@resend.dev to the account's own email (400 validation_error otherwise), which is enough for the Phase 2 check. For real recipients: MANUAL https://resend.com/domains -> Add Domain (e.g. mail.<owned-domain>, region us-east-1; enable Receiving for Phase 5 inbound) -> add DKIM/SPF (+MX) records at the DNS host -> Verify (<=72h); or via CLI once the bootstrap key exists: `resend login --key re_...` (resend-cli 2.18.0, node>=22) then `resend domains create --name mail.<domain> --region us-east-1` ; `resend domains verify <id>` ; `resend api-keys create --name papaflow-prod --permission sending_access`. Alternative: `vercel integration add resend --plan free -m domain=<owned-domain> -m region=us-east-1 -e production -e preview -e development` injects RESEND_API_KEY but needs an owned domain and a TTY `vercel integration accept-terms resend` on first install.
  RESOLUTION: SECRET-FROM-USER (bootstrap key) and MANUAL (domain DNS). Free tier: 3,000/month, 100/day, inbound counts.
- [major] P4.1 SECRET-FROM-USER: model-provider keys for 'Test connection' (Phase 4)
  These are per-org secrets entered through the app's connections UI (sealed with CREDENTIALS_KEK), not env vars: user supplies their own OpenAI (sk-...), Anthropic (sk-ant-...), Gemini (AIza...), Groq (gsk_...) keys to exercise validate/list endpoints (GET /v1/models etc.). No app-level provisioning. Optional AI Gateway BYOK path needs `AI_GATEWAY_API_KEY` from Vercel dashboard > AI Gateway (paid tier for BYOK; failing user keys silently fall back to system credits).
  RESOLUTION: SECRET-FROM-USER at test time. Nothing to provision; CREDENTIALS_KEK from P0.7 must be in .env.local before this phase.
- [major] P5.1 SECRET-FROM-USER + CLI: Telegram bot and Stripe test webhook (Phase 5 triggers)
  Telegram: user opens @BotFather -> /newbot -> token; pasted into PapaFlow (per-connection secret), app calls getMe then `setWebhook` with url `https://<APP_ORIGIN>/api/events/telegram/<connectionId>` and a generated secret_token (header X-Telegram-Bot-Api-Secret-Token). Requires the deployed HTTPS origin from P0.9 (Telegram accepts only HTTPS on ports 443/80/88/8443); for local dev use `ngrok http 3000` or `cloudflared tunnel --url http://localhost:3000`. Stripe: local binary is 1.41.2 (`brew upgrade stripe` or `npm i -g @stripe/cli` -> 1.50.8 optional); `stripe login` (browser); `stripe listen --forward-to localhost:3000/api/events/stripe/<connectionId>` prints whsec_ (stable across restarts) which the user pastes into PapaFlow; `stripe trigger payment_intent.succeeded`. Production: user creates the endpoint at https://dashboard.stripe.com/webhooks pointing at `https://<APP_ORIGIN>/api/events/stripe/<connectionId>` and pastes the revealed whsec_. Form and generic webhook triggers need nothing beyond the deployed origin.
  RESOLUTION: SECRET-FROM-USER (bot token, whsec_) plus interactive `stripe login`. No env vars; everything is per-connection in the vault.
- [major] P6.1 SECRET-FROM-USER + MANUAL: Discord application (Phase 6)
  User at https://discord.com/developers/applications -> New Application -> General Information: copy Application ID -> DISCORD_APP_ID, Public Key -> DISCORD_PUBLIC_KEY; Bot tab -> Reset Token -> DISCORD_BOT_TOKEN. Put all three in .env.local and `vercel env add ... production|preview|development`. Interactions Endpoint URL = `https://<APP_ORIGIN>/api/events/discord` can only be saved AFTER the route is deployed (Discord POSTs {type:1} plus invalid-signature probes and requires Ed25519 verification + 401). Invite URL for users: `https://discord.com/oauth2/authorize?client_id=<DISCORD_APP_ID>&scope=bot%20applications.commands&permissions=3072` (19456 with embeds). Register the slash command after deploy: `curl -X PUT https://discord.com/api/v10/applications/<APP_ID>/guilds/<GUILD_ID>/commands -H 'Authorization: Bot <TOKEN>' ...`. Discord webhook URLs (pasted by users) need no provisioning.
  RESOLUTION: SECRET-FROM-USER + MANUAL portal steps. Order: deploy route first, then save the Interactions Endpoint URL.
- [major] P7.1 SECRET-FROM-USER + MANUAL: Slack app from manifest (Phase 7, used again in Phase 8)
  User at https://api.slack.com/apps -> Create New App -> From a manifest -> paste manifest with oauth_config.redirect_urls [`https://<APP_ORIGIN>/api/oauth/slack/callback`, plus a tunnel host for local dev; HTTPS only, localhost cannot be registered], oauth_config.scopes.bot [chat:write, chat:write.public, channels:read, app_mentions:read], settings.event_subscriptions.request_url `https://<APP_ORIGIN>/api/events/slack` with bot_events [app_mention], settings.interactivity.is_enabled true + request_url `https://<APP_ORIGIN>/api/events/slack` (Phase 8 Approval buttons), settings.token_rotation_enabled false (never enable; irreversible). Basic Information -> copy Client ID / Client Secret / Signing Secret -> SLACK_CLIENT_ID, SLACK_CLIENT_SECRET, SLACK_SIGNING_SECRET into .env.local and `vercel env add` for production/preview/development. The Events request URL only saves once the deployed route answers url_verification, so deploy the route before saving the manifest's events section (or add it in a second edit). Invite the bot to a channel before testing app_mention.
  RESOLUTION: SECRET-FROM-USER + MANUAL. Depends on APP_ORIGIN from P0.9 and deployed routes.
- [major] P7.2 SECRET-FROM-USER + MANUAL: Notion public integration and Airtable OAuth integration (Phase 7)
  Notion: https://app.notion.com/developers/connections -> New connection -> Distribution Public -> redirect URI `https://<APP_ORIGIN>/api/oauth/notion/callback` (cannot be changed afterwards; add a tunnel URI too if local testing is wanted) -> copy OAuth Client ID / Secret -> NOTION_CLIENT_ID / NOTION_CLIENT_SECRET. Webhook trigger (optional): same connection -> Webhooks tab -> subscription URL `https://<APP_ORIGIN>/api/events/notion` -> paste back the verification_token Notion POSTs (UI-only). Airtable: https://airtable.com/create/oauth -> name, redirect URL `https://<APP_ORIGIN>/api/oauth/airtable/callback`, scopes data.records:read data.records:write schema.bases:read webhook:manage -> Generate client secret (shown once) -> AIRTABLE_CLIENT_ID / AIRTABLE_CLIENT_SECRET. Linear (API key per user) and GitHub (fine-grained PAT per user) need no app-level provisioning; Linear OAuth (24h tokens, rotating refresh) only if built: https://linear.app/settings/api/applications/new -> LINEAR_CLIENT_ID/SECRET. Push all four into .env.local and `vercel env add` (production, preview, development separately).
  RESOLUTION: SECRET-FROM-USER + MANUAL. Requires APP_ORIGIN; redirect URIs are immutable on Notion so decide the final domain first (see ORDER RISKS).
- [major] P10.1 CLI + SECRET-FROM-USER: eve install, Next wiring, auth channel, model credentials (Phase 10; spike first)
  Spike in a scratch folder first (CLAUDE.md line 111): `npx eve@latest init spike` is allowed there. Main repo: `pnpm add eve@0.49.0` (never eve@beta = stale 0.6.0-beta.20; re-check `npm view eve version`), rename next.config.ts -> next.config.mts and `export default withEve(withWorkflow(nextConfig), { agents: { runtime: "./agents/runtime" } })` (composition order is undocumented; verify /eve/agents/runtime/eve/v1/health AND .well-known/workflow routes under `next dev` and `vercel deploy --dry`). Exclude `/eve/(.*)` from the Clerk proxy matcher. Add agents/runtime/channels/eve.ts with `eveChannel({ auth: [clerkAuth(), localDev()] })` BEFORE the first preview deploy (production fails closed). Model credentials for eve: either a Gateway string model with `AI_GATEWAY_API_KEY` (Vercel dashboard > AI Gateway > API keys; or `vercel env pull` for OIDC on Vercel) or a provider key passed from a step.started handler; the Agent node uses the org's own key. Env: APP_ORIGIN (eve/client host `${APP_ORIGIN}/eve/agents/runtime`) in .env.local and Vercel. `next dev` spawns the eve dev server itself; no second terminal.
  RESOLUTION: CLI plus SECRET-FROM-USER (AI_GATEWAY_API_KEY or a house provider key). package.json engines 24.x from P0.2 satisfies eve's node>=24.
- [major] P11.1 CLI: Clerk Billing for organizations, plans and features (Phase 11)
  `clerk enable billing --for orgs --yes --no-skills` (sets billing.organization_enabled; auto-creates default org plan slug `free_org` -> key PLAN_LIMITS on free_org; requires a claimed non-keyless app; dev uses Clerk's shared test Stripe gateway, no Stripe account needed). Then `clerk config schema --keys billing` and `clerk config patch --json '{"billing":{"features":{"core_connectors":{"name":"Core connectors"},"pro_connectors":{"name":"Pro connectors"},"ai_agent":{"name":"AI agent"},"ai_builder":{"name":"AI builder"},"schedules":{"name":"Schedules"},"run_history_30d":{"name":"30-day run history"},"shared_connections":{"name":"Shared connections"},"audit_log":{"name":"Audit log"},"priority_runs":{"name":"Priority runs"}},"plans":{"free_org":{"features":["core_connectors"]},"pro":{"name":"Pro","payer_type":"org","amount":2900,"annual_monthly_amount":2400,"currency":"usd","free_trial_enabled":true,"free_trial_days":7,"features":["core_connectors","pro_connectors","ai_agent","ai_builder","schedules","run_history_30d"]},"team":{"name":"Team","payer_type":"org","amount":9900,"currency":"usd","features":["core_connectors","pro_connectors","ai_agent","ai_builder","schedules","run_history_30d","shared_connections","audit_log","priority_runs"]}}}}' --dry-run` then `--yes`; verify `clerk api /billing/plans`. MANUAL: Dashboard > Subscription plans > Plans for Organizations > Team > Seat-based (no config-schema keys; >20 seats or unlimited needs the $100/mo B2B add-on; @clerk/nextjs >=7.5.1 ok). The webhook from P0.8 already subscribes subscription*/subscriptionItem* events.
  RESOLUTION: CLI with one MANUAL seat-based step. Enabling billing also flips organization_settings.enabled, harmless since P0.4 already did.
- [major] P11.2 MANUAL + SECRET-FROM-USER: Clerk production instance, custom domain, own Stripe (Phase 11 / launch)
  Until now production on Vercel runs on the Clerk development instance keys (pk_test_/sk_test_), which is fine for demos but not for real billing. For production: user must own a domain and control DNS. `clerk deploy` (guided dev->prod clone; interactive) then `clerk deploy status --wait`; DNS CNAMEs clerk.<domain>, accounts.<domain>, clkmail.<domain> (up to 48h). Then `clerk env pull --instance prod --file .env.production.local` -> `vercel env add NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY production --value pk_live_... --yes --force` and CLERK_SECRET_KEY likewise. Re-activate the Convex integration on the prod instance (P0.6, dashboard) -> `npx convex env set --prod CLERK_FRONTEND_API_URL https://clerk.<domain>`; create the prod webhook endpoint (P0.8) -> `npx convex env set --prod CLERK_WEBHOOK_SIGNING_SECRET`. Billing in production needs the user's own Stripe account connected in the Clerk Dashboard (production instance > Billing settings); *.vercel.app cannot be a Clerk production domain. Add the custom domain on Vercel: `vercel domains add <domain>` (DNS at the registrar is MANUAL). Update every OAuth redirect URI / event URL from P5-P7 to the custom domain (Notion's cannot be edited; create a new connection).
  RESOLUTION: MANUAL + SECRET-FROM-USER (domain, Stripe account). Optional until real customers; marked major because Phase 11's check on a *.vercel.app deploy can only exercise the dev instance's test gateway.
- [major] P12.1 SECRET-FROM-USER + CLI: Builder agent env (Phase 12)
  `HOUSE_AI_API_KEY` (Builder fallback model, per-org cap; user's own Anthropic or OpenAI key — Anthropic 5-series rejects temperature/forced tool choice, so use adaptive defaults) -> .env.local and `vercel env add HOUSE_AI_API_KEY production|preview|development --value ... --yes`. Add `builder: "./agents/builder"` to the withEve agents map, channels/eve.ts auth for the builder agent, static durable tools under agents/builder/tools/. Run the second spike (ask() round-trip to a custom React widget) before this phase. Chat panel: `useEveAgent({ agent: "builder", headers: async () => ({ authorization: `Bearer ${await getToken()}` }) })`.
  RESOLUTION: SECRET-FROM-USER + CLI. Nothing else to provision; reuses P10.1 wiring.
- [info] OPTIONAL: Vercel Blob store for image/audio node outputs
  If Image/TTS nodes store binaries (PLAN mentions @vercel/blob): `pnpm add @vercel/blob@2.8.0` ; `vercel blob create-store papaflow-media --access public --yes --environment production --environment preview --environment development` (NOT `blob store add`) -> injects BLOB_STORE_ID, VERCEL_OIDC_TOKEN, BLOB_WEBHOOK_PUBLIC_KEY; `vercel env pull .env.local --yes` for local OIDC (12h tokens, auto-refreshed). `put(pathname, body, { access: 'public' })` — access is required.
  RESOLUTION: CLI, only when a node needs it (Phases 4+).
- [info] OPTIONAL: Vercel Marketplace route instead of CLI-direct (Convex/Clerk/Resend)
  Not recommended for this project but documented: first-time TTY `vercel integration accept-terms convex|clerk|resend` (human), then `vercel integration add convex --plan CONVEX_BASE -m region=default -m defaultRegion=true -e production -e preview` (creates a Convex project in a SEPARATE marketplace-owned Convex team; local dev then needs browser `npx convex login --vercel`; syncs CONVEX_DEPLOY_KEY prod+preview automatically) ; `vercel integration add clerk --plan hobby_2025_08 -e production -e preview -e development` (provisions a NEW Clerk app you cannot merge with a CLI-created one; Organizations still need `clerk link --app` + `clerk enable orgs`; dev instance -> development+preview, prod -> production) ; `vercel integration add resend --plan free -m domain=<owned> -m region=us-east-1`. Also optional: `vercel upgrade` to 59.x gains `vercel project update --build-command`, `env add NAME production,preview`, but 55+ requires `--scope/--team` on non-TTY link; 59.x changelog is unverifiable.
  RESOLUTION: Skip unless the user prefers Vercel-managed billing for these vendors.
- [info] OPTIONAL: local dev conveniences
  HTTPS tunnel for Slack/Telegram/Discord local testing: `ngrok http 3000` or `cloudflared tunnel --url http://localhost:3000` (tunnel host must be added to Slack redirect_urls / Notion redirect URIs). `clerk webhooks listen --token "$(clerk webhooks token)" --forward-to http://localhost:3000/api/webhooks` if a Next-side Clerk webhook is ever wanted (the Convex .convex.site endpoint does not need it). `vercel env pull .env.local --yes` after marketplace/blob steps (on 54.x it overwrites local-only vars; back up .env.local first). If Vercel Deployment Protection is on, `eve dev <url>` needs VERCEL_AUTOMATION_BYPASS_SECRET.
  RESOLUTION: CLI, as needed.
- [info] ENV MATRIX
  Format: VAR — where set — first phase — source.
.env.local only (Next dev): NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY (P1, `clerk env pull`) ; CLERK_SECRET_KEY (P1, `clerk env pull`) ; CONVEX_DEPLOYMENT=dev:<name> (P1, written by `npx convex dev`; NEVER on Vercel) ; NEXT_PUBLIC_CONVEX_URL (P1, written by `npx convex dev`; on Vercel injected by `npx convex deploy` at build, never set manually) ; WORKFLOW_TARGET_WORLD/WORKFLOW_LOCAL_DATA_DIR (P2, optional).
.env.local AND Vercel (production, preview, development each via separate `vercel env add`): NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY + CLERK_SECRET_KEY (P1; dev keys everywhere until P11.2 swaps production to pk_live_/sk_live_) ; ENGINE_SECRET (P2) ; CREDENTIALS_KEK (P4) ; RESEND_API_KEY (P2; user) ; DISCORD_APP_ID, DISCORD_PUBLIC_KEY, DISCORD_BOT_TOKEN (P6; user) ; SLACK_CLIENT_ID, SLACK_CLIENT_SECRET, SLACK_SIGNING_SECRET (P7; user) ; NOTION_CLIENT_ID, NOTION_CLIENT_SECRET (P7; user) ; AIRTABLE_CLIENT_ID, AIRTABLE_CLIENT_SECRET (P7; user) ; APP_ORIGIN (P5 for trigger URLs, required P10 for eve/client host) ; AI_GATEWAY_API_KEY (P10, optional; user) ; HOUSE_AI_API_KEY (P12; user) ; RESEND_WEBHOOK_SECRET (P5 inbound email, optional; user) ; LINEAR_CLIENT_ID/SECRET (only if Linear OAuth is built).
Vercel only: CONVEX_DEPLOY_KEY (P1 first deploy; production-scoped prod key + preview-scoped preview key; both dashboard-generated) ; BLOB_STORE_ID, VERCEL_OIDC_TOKEN, BLOB_WEBHOOK_PUBLIC_KEY (optional, injected by `vercel blob create-store`; pull to .env.local with `vercel env pull`).
Convex deployment (`npx convex env set`, repeat with `--prod` for production): CLERK_FRONTEND_API_URL (P1; issuer for auth.config.ts; Convex docs call it CLERK_JWT_ISSUER_DOMAIN — pick one name and use it in auth.config.ts) ; CLERK_WEBHOOK_SIGNING_SECRET (P1; verifyWebhook reads this exact name) ; ENGINE_SECRET (P2; same value as Next side). Convex auto-provides CONVEX_SITE_URL/CONVEX_CLOUD_URL (1.44+).
Per-connection secrets in the vault, never env: provider AI keys (P4), Telegram bot token + secret_token (P5), Stripe whsec_ (P5), Discord webhook URLs (P6), Slack/Notion/Airtable OAuth tokens (P7), Linear API keys, GitHub PATs, GitHub/Airtable webhook secrets, Teams workflow URLs, Twilio API key SID/secret + Auth Token.
CLAUDE.md line 115 correction: CONVEX_DEPLOYMENT and NEXT_PUBLIC_CONVEX_URL are local-only; CONVEX_DEPLOY_KEY (missing from the list) is the Vercel-side var; CLERK_FRONTEND_API_URL (missing) is the Convex-side var.
  RESOLUTION: Use this as the checklist for `vercel env ls`, `npx convex env list`, and .env.local before each phase.
- [info] GAPS
  Steps the findings do not fully explain; research at execution time:
1. Clerk Convex-integration activation has no CLI/API path — dashboard only; after activating, run `clerk config pull --keys session` to learn where aud is stored and whether `clerk config patch` can replicate it for the prod instance.
2. Clerk webhook endpoint creation via CLI is contradictory (docs say `clerk api` can; CLI 3.2.0 shows only /webhooks/svix routes) — run `clerk api ls webhook` after `clerk update` (3.3.0); fall back to dashboard.
3. `clerk apps create` response JSON shape is undocumented — read application_id from the actual output.
4. Convex identity claim shape for Clerk v2 tokens: whether nested `o` arrives as identity['o.id'] (dot-flattened), a stringified JSON, or an object is unresolved; whether `{{org.id}}`/`{{org.role}}` shortcodes exist in the Claims editor is unverified (only `{{org.name}}` is documented) — log `ctx.auth.getUserIdentity()` once in dev before writing requireOrg().
5. Whether svix 2.2.0 (engines node>=22, ESM-only) bundles into Convex's default runtime — preferred fix is `verifyWebhook` from `@clerk/backend/webhooks`; confirm the first `npx convex dev --once` push.
6. `--cmd-url-env-var-name` plus `withWorkflow` under `npx convex deploy --cmd 'pnpm build'` on Vercel is untested — verify on the first deploy that runs appear in `npx workflow inspect runs --backend vercel --project papaflow --team sonnysanghas-projects --env production`.
7. withEve(withWorkflow()) composition order is undocumented on both sides — spike before Phase 10.
8. eve `Client` addressing of a named agent (host prefix `/eve/agents/runtime`) and Clerk session-token verification inside channels/eve.ts (`verifyToken` from @clerk/backend, Core 3 renamed verify functions) — confirm exports at install.
9. Whether Convex `project create` / `deployment create` flags match the docs on convex 1.45.0 — check `--help` before relying on them; the `dev --configure new` path is the verified one.
10. Vercel CLI 59.x behaviour is unverifiable (no tags/changelog past 58.4.4); stay on 54.18.6 or diff `--help` output after `vercel upgrade`.
11. Convex preview deploy keys and the 'project token' generation location — dashboard only; exact dashboard labels may differ.
12. Resend: number of webhook endpoints on the free plan and inbound max message size — unverified.
13. Anthropic GET /v1/models: whether it returns alias rows (claude-haiku-4-5) or dated ids — persist whatever comes back.
14. Teams Workflows trigger: whether a bare {"text":...} body renders — test with an Adaptive Card first.
15. Slack app_mention/Events URL verification requires the deployed route to exist — no way to pre-register; sequence the manifest edit after deploy.
16. Clerk seat-based plan settings have no config-schema keys — dashboard only; verify the B2B add-on requirement at the time.
17. Whether `clerk enable orgs` on a freshly created (unclaimed?) app succeeds non-interactively — `clerk apps create` from an authenticated session should yield a claimed app; if the CLI reports keyless/accountless, claim it in the dashboard first.
  RESOLUTION: Each gap is a one-command or one-dashboard check at execution time; none blocks the scaffold.
- [info] ORDER RISKS
  Hard sequencing constraints:
1. Scaffold BEFORE copying CLAUDE.md into the directory (create-next-app aborts on CLAUDE.md/AGENTS.md; `docs/` is allowed).
2. Clerk app (P0.4) BEFORE the Convex integration activation (P0.6) and BEFORE `clerk enable orgs`/billing (both need a linked app).
3. Convex deployment (P0.5) BEFORE `npx convex env set CLERK_FRONTEND_API_URL`, BEFORE writing/pushing auth.config.ts (it reads that env), and BEFORE the Clerk webhook endpoint (URL needs `<deployment>.convex.site`).
4. Frontend API URL (from the Clerk dashboard activation) is required BEFORE the first auth.config.ts push; pushing auth.config.ts with an unset env var fails the deploy.
5. Write convex/schema.ts and convex/http.ts BEFORE the webhook endpoint is created in Clerk, otherwise Clerk's first deliveries 404 (retried, but noisy).
6. ENGINE_SECRET must be set on BOTH Convex (`npx convex env set`) and Next (.env.local/Vercel) before Phase 2's first run; a mismatch fails every step write.
7. Convex production deployment does not exist until the first `npx convex deploy` with a production key — generate the Production Deploy Key from the dashboard (which creates/exposes the prod deployment) BEFORE the first `vercel deploy`, and set `--prod` Convex env vars (CLERK_FRONTEND_API_URL, CLERK_WEBHOOK_SIGNING_SECRET, ENGINE_SECRET) BEFORE that build or the prod push of auth.config.ts fails.
8. First `vercel deploy` of a new project is ALWAYS production — have CONVEX_DEPLOY_KEY (production) and all production env vars in place before running it.
9. GitHub repo + GitHub App install BEFORE `vercel git connect --yes`.
10. Stable HTTPS origin (Vercel) BEFORE Telegram setWebhook, Discord Interactions Endpoint, Slack redirect/event URLs, Notion redirect URI (immutable once saved), Airtable redirect URL, eve Client host — Phases 5-7, 10, 12. If a custom domain is planned (P11.2), decide it before Phase 7 or you will re-register Slack/Airtable and re-create the Notion connection.
11. Deployed route BEFORE saving Slack Events request_url (url_verification) and Discord Interactions Endpoint URL (PING + signature probes) — deploy, then edit the manifest/portal.
12. proxy.ts matcher exclusions: `.well-known/workflow/` at Phase 2 (before the first workflow run) and `/eve/(.*)` at Phase 10 (before eve dev traffic) — forgetting either produces silent queue/auth failures.
13. `clerk enable billing --for orgs` creates plan slug `free_org` — PLAN_LIMITS keys and any `has({ plan })` checks must use free_org/pro/team; run it BEFORE writing Phase 11 code that hardcodes slugs.
14. Clerk prod instance (`clerk deploy`) requires an owned domain with DNS; do it BEFORE production billing and BEFORE swapping Vercel production Clerk keys; then re-do P0.6 and P0.8 for the prod instance (they are per instance).
15. eve production fails closed: channels/eve.ts auth must be committed BEFORE the first preview/production deploy that includes withEve, or /eve/** returns 401.
16. Do not rename/move workflows/steps/run-node.ts after production runs exist (deploymentId:'latest' upgrades and observability names break); pick final paths in Phase 2.
17. CREDENTIALS_KEK must be generated once and present in every environment before the first connection is sealed (Phase 4); rotating it later needs the keyId migration path.
18. package.json `engines.node: 24.x` must be present before adding eve (npm engine check) and before the first Vercel build, so set it in P0.2.
  RESOLUTION: Follow the item numbering P0.1 -> P0.9 -> P2 -> P4 -> P5 -> P6 -> P7 -> P10 -> P11 -> P12; every constraint above is satisfied by that order.
